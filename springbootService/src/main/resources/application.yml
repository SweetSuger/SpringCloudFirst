spring:
  application:
    name: springbootService
  cloud:
    config:
      uri: http://localhost:5002/
      profile: release
server:
  port: 6003
security:
  basic:
    enabled: false
eureka:
  client:
    service-url:
      defaultZone: http://localhost:5000/eureka/
